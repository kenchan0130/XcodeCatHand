# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'XcodeCatHand' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for XcodeCatHand
  pod "SwiftyPlist", git: 'https://github.com/kenchan0130/SwiftyPlist.git'

  target 'XcodeCatHandTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'XcodeCatHandUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
